name: glassmorphism_ui
description: A Flutter package for creating beautiful glassmorphism UI effects.
version: 0.0.1
homepage: https://github.com/MaheshSatre16/glassmorphism_ui

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
